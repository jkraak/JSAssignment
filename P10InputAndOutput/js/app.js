const numberToSquare= 4
function squareNumber(numberToSquare) {
  return numberToSquare * numberToSquare
}

console.log(squareNumber(numberToSquare))

function average (number1,number2) {
  return (number1 + number2) /2
}

console.log (average(2,6));


function greeting(firstName, SurName) {
  return "Welcome " + firstName+ " " + SurName + "!"
}
const firstName = "Jenny";
const surName = "Berglund"
console.log(greeting(firstName, surName));
